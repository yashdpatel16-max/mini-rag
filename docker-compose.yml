# version: "3.9"

services:

  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - qdrant
      - mongodb

  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"

  mongodb:
    image: mongo
    ports:
      - "27017:27017"